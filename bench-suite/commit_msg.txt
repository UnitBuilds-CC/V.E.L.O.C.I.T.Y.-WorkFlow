fix(bench-suite): add missing bench-client service and wal_group_commit workload

- Add bench-client service to docker-compose.yml (spec requirement)
  Uses velocity-bench-client Dockerfile, depends on all 6 engine services,
  uses 'bench' profile so it only starts when explicitly requested.
- Add wal_group_commit workload to velocity-bench (spec requirement)
  Maps to WalDurability kind with 1000 sequential ops measuring
  group commit amortization. Complements existing wal_durability workload.

Phase 2c verification: all per-engine clients confirmed to support
full workload matrix with configurable URLs and --profile flag.
