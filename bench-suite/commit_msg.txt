feat(bench-suite): add CLI flags to per-engine benchmark clients

Phase 2c: Update per-engine clients for bench-suite integration.

- DBOS client: add --base-url, --output, argparse CLI
- Temporal client: add --base-url, --output, argparse CLI
- Restate client: add --ingress, --output, manual arg parsing

All clients now support configurable service URLs and output paths
for use with the bench-suite orchestrator scripts.
