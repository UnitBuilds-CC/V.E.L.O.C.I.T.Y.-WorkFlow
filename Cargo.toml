[workspace]
members = [
    "velocity-workflow-core",
    "velocity-workflow-engine",
    "velocity-workflow-daemon",
]
resolver = "2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
