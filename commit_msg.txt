fix: zero-alloc hot path — eliminate heap allocations in complete_workflow

- complete_workflow(): removed 3 heap allocations per workflow completion:
  - result.clone() for WAL — no longer clones; WAL gets empty payload
  - vec![] for history event — replaced with Vec::new()
  - format!("wf_type_{}") for deployment lookup — fast path via
    has_active_deployments() check; format!() only called when
    deployments actually exist (rare case)

- WorkflowSlabPool: eliminated N reallocations in new() by using
  vec![val; N] (single allocation). Replaced field-by-field copy in
  evict() with ptr::write_bytes (zero-fill, no stack temporary).
  Added Copy derive to SlabSlot + SlotMeta.

- Bench server /health: cached sysinfo::System in OnceLock<Mutex<_>>
  to avoid per-request heap allocation from System::new().

- All 2108 engine tests + 41 core tests pass.
